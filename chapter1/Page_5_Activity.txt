Create a string named name with the value "Fido".
Create an object called myDog from the Dog class with properties name and size.
Set a variable x to the value of size (which is 25) minus 5.
While x is greater than 3:

    Call the play method of the myDog object.
    Continue the loop.

Create an array named numList with the values 2, 4, 6, and 8.
Print "Dog: Fido".
Create a string named num with the value "8".
Create an integer named z, and set it to the integer value of num.
Use a try-catch block to:

    Try opening the file myFile.txt for reading.
    If it fails, catch the FileNotFoundException and print "File not found".